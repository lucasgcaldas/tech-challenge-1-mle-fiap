# tech-challenge-1-mle-fiap

## Set up the project

- ```$ python -m venv venv```
- ```$ pip install -r requirements.txt```
- ```$ uvicorn app.main:app --reload```
- To access the application -> [http://localhost:8000/docs/](http://localhost:8000/docs/)